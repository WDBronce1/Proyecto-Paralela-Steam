package client;

public class RunClient {
    // Este metodo tiene como objetivo iniciar el cliente de la aplicacion
    public static void main(String[] args) {
        Client client = new Client();
        client.startClient();
    }
}
